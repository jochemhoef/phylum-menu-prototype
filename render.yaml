services:
  - type: web
    name: phylum-menu
    runtime: static
    buildCommand: echo "Static site — no build step"
    staticPublishPath: .
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
